@model Libreria.Models.Categoria

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-center">Detalles de Categoría</h1>

<div class="form-container">
    <div class="col-md-4 form-background">
        <h4 class="text-center">Categoría</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-4 control-label">
                @Html.DisplayNameFor(model => model.Nombre)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.Nombre)
            </dd>
        </dl>
        <div class="d-flex justify-content-end">
            <a asp-action="Edit" asp-route-id="@Model?.CodigoCategoria" class="btn btn-info">Editar</a>
            <a asp-action="Index" class="btn btn-secondary">Volver</a>
        </div>
    </div>
</div>
